part of './bloc.dart';
/*
enum MapStatus { loading, success, failure, detail }

class MapState extends Equatable {
   final MapStatus status;
  final MapModel map;

  const MapState({
    this.status = MapStatus.loading,
    this.map = const MapModel()
  });

  MapState copyWith({
    MapStatus? status,
    MapModel? map
  }) => MapState(
    status: status ?? this.status,
    map: map ?? this.map
  );

  @override
  List<Object> get props => [status, map];
}*/
